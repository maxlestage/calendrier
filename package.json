{
  "name": "calendrier",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install --include=dev && npm run build && rm -rf node_modules"
  }
}
